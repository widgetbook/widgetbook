name: widgetbook

on:
  pull_request:
    paths:
      - packages/widgetbook/**
      - .github/workflows/widgetbook.yaml
  push:
    branches:
      - main

jobs:
  build:
    uses: ./.github/workflows/base.yml
    with:
      working_directory: packages/widgetbook
