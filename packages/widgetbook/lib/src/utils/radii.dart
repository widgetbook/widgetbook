import 'package:flutter/material.dart';

const double mainRadiusValue = 4;
const Radius mainRadius = Radius.circular(mainRadiusValue);
const BorderRadius mainBorderRadius = BorderRadius.all(mainRadius);

const double canvasRadiusValue = 6;
const Radius canvasRadius = Radius.circular(canvasRadiusValue);
const BorderRadius canvasBorderRadius = BorderRadius.all(canvasRadius);
