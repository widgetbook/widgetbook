import 'package:flutter/material.dart';

class Radii {
  // TODO not all border radii use this method
  // TODO find and replace
  static const BorderRadius defaultRadius = BorderRadius.all(
    Radius.circular(
      6,
    ),
  );
}
