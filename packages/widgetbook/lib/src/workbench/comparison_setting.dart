/// An enum identifying for which setting the Workbench shows all the renders
enum ComparisonSetting {
  /// No comparison has been selected
  none,

  /// Allows to compare themes
  themes,

  /// Allows to compare devices
  devices,

  /// Allows to compare localization
  localization,

  /// Allows to compare text scales
  textScale
}
