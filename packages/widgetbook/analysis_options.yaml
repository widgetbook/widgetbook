include: ../../analysis_options.yaml

formatter:
  trailing_commas: preserve

linter:
  rules:
    # API docs disabled for now until beta version
    public_member_api_docs: false
analyzer:
  exclude:
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
