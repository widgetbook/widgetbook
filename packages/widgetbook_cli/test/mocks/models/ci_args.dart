import '../../../bin/models/models.dart';

const actor = 'John Doe';
const repoName = 'widgetbook';
const vendor = 'GitHub';

const ciArgs = CiArgs(
  actor: actor,
  repository: repoName,
  vendor: vendor,
);
