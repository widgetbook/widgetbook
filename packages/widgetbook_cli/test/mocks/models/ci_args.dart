import '../../../bin/models/models.dart';

const actor = 'John Doe';
const repository = 'widgetbook';
const vendor = 'GitHub';

const ciArgs = CiArgs(
  actor: actor,
  repository: repository,
  vendor: vendor,
);
