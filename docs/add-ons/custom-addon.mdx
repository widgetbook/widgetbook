# Overview

Widgetbook allows developers to define their own Add-on. 
Therefore, Widgetbook is fully customizable to your needs. 

<Warning>
    As we are currently refining the API, the documentation is a bit sparse.
</Warning>

## Setup

<Tabs
  groupId="approach"
  values={[
    { label: 'Generator', value: 'generator' },
    { label: 'Manual', value: 'manual' },
  ]}
>
  <TabItem value="generator">
    Not yet supported, but coming soon.
  </TabItem>
  <TabItem value="manual">
    Create a new class that extends the `WidgetbookAddOn` class with a generic parameter that is of type `WidgetbookAddOnModel`. 
    Create the model that extends `WidgetbookAddOnModel`. 

    <Warning>
        More infos coming soon.
    </Warning> 
  </TabItem>
</Tabs>