# Accessibility Addon

The Accessibility Addon helps detect accessibility issues using
the [`accessibility_tools`](https://pub.dev/packages/accessibility_tools) package.

## Setup

Here's how to configure it:

```dart
AccessibilityAddon(),
```

## Example usage

<Video autoPlay muted src="/assets/screenshots/addons/accessibility-addon.mov" type="video/mp4" />
