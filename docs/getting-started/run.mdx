# Run

`Widgetbook` is supported on the following environments.

| Environment | Status | Comment                                                                                           |
| ----------- | ------ | ------------------------------------------------------------------------------------------------- |
| MacOS       | ✅     |
| Windows     | ✅     |
| Linux       | ✅     |
| Web         | ✅     | No hot reload, but hot restart (see [Issue 4](https://github.com/widgetbook/widgetbook/issues/4)) |
| Mobile      | ➖     | Will run, but is not optimized. If you see a usecase for `Widgetbook` on mobile let us know.      |

See the [Desktop support for Flutter](https://docs.flutter.dev/desktop) page for setup instructions.

Check if everything works by running `flutter run` with the newly created Widgetbook:

<Info>
  If you have renamed `ui_catalogs.widgetbook.dart`, ensure that you pass correct name to `-t` as
  entry point.
</Info>

<Tabs
  values={[
    { label: 'MacOS', value: 'macos' },
    { label: 'Windows', value: 'windows' },
    { label: 'Linux', value: 'linux' },
    { label: 'Web', value: 'web' },
    { label: 'Mobile', value: 'mobile' },
  ]}
>
  <TabItem value="macos">
    ```bash flutter run -d macos -t lib/ui_catalogs.widgetbook.dart ```
  </TabItem>
  <TabItem value="macos">
    ```bash flutter run -d windows -t lib/ui_catalogs.widgetbook.dart ```
  </TabItem>
  <TabItem value="macos">
    ```bash flutter run -d linux -t lib/ui_catalogs.widgetbook.dart ```
  </TabItem>
  <TabItem value="web">
    ```bash flutter run -d chrome -t lib/ui_catalogs.widgetbook.dart ```
  </TabItem>
  <TabItem value="web">
    It will run, but is not optimized. If you see a use-case for Widgetbook on mobile let us know.
  </TabItem>
</Tabs>

<Image
  zoom
  src="/assets/screenshots/empty_project.png"
  alt="Empty Widgetbook"
  caption="Empty Widgetbook"
/>

<Success>
  Widgetbook is now running, but no components are shown yet. To learn how to add components and how
  to configure Widgetbook to your liking, view the [configuration
  documentation](/getting-started/configuration).
</Success>

You can check the final getting started on Zapp!

<Zapp id="zl406qol506" />
